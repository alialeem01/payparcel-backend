D:
cd D:\
mkdir payparcel-project
cd payparcel-project

python -m venv venv
venv\Scripts\activate
pip install django

git add .
git commit -m "Fix filter text visibility, hide sidebar headings, tighten spacing"
git push

python manage.py makemigrations
python manage.py migrate

localStorage.clear(); location.reload();